# Exclude extracted folder (duplicates of root files)
extracted/

# Exclude original zip file
*.zip

# Exclude files with double extensions (originals)
*.png.png

# Exclude phonto (temp file)
phonto.PNG

# Local Netlify folder
.netlify

# Node deps (not used at runtime — Netlify serves static files)
node_modules/

# Local Claude Code settings
.claude/

# Local backups
*.splash-backup
*.bak

# Command Center static export — generated by rileyai-website `npm run export:command`
# /_next/  — MUST ship: the Command Center (Next.js static build) lives on these chunks; ignoring it made every git deploy replace the site without CC assets (white screen)
